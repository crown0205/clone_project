import React from "react";
import styled from "styled-components";

const Detail = () => {
    return (
        <React.Fragment>
            <h1>Detail</h1>
        </React.Fragment>
    );
};

export default Detail;
