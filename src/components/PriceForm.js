import React from "react";

const PriceForm = (props) => {
    return (
        <React.Fragment>
            <dl>
                <dt>상품금액</dt>
                <span>0</span>
                <span>원</span>
            </dl>
        </React.Fragment>
    );
};

export default PriceForm;
