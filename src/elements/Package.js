import React from "react";
import styled from "styled-components";

const Package = (props) => {
    return (
        <React.Fragment>
            <div>
                <span></span>
                <span></span>
            </div>
            <div>
                <button></button>
            </div>
        </React.Fragment>
    );
};

export default Package;
